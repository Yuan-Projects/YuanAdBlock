$(function(){
    // Remove footer AD
    $('table:last').remove();  

    // Remove header AD
    $('table[bgcolor="#666666"]').remove();

    // Remove Rank list
    $('table.topContent_MainTable').remove();
});
